const slides = document.querySelectorAll(".carousel__photo");
const prev_button = document.querySelector(".carousel__button--prev");
const next_button = document.querySelector(".carousel__button--next");
const slide = 0;
